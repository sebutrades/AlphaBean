"""
simulation/custom/ — Custom Agent Trading system.

Background-persistent simulation engine with:
  - Strategy filtering (select which patterns to trade)
  - Adaptive position sizing (compound equity, strategy-adjusted)
  - Configurable agent deliberation (quick/standard/thorough)
  - Per-agent model selection (haiku/sonnet/opus)
  - Persistent run logging with comparison
  - WebSocket-independent execution (navigate away without stopping)
"""
